import { CategoryPosition } from './category-position.model';

describe('CategoryPosition', () => {
  it('should create an instance', () => {
    expect(new CategoryPosition()).toBeTruthy();
  });
});
